ranking { 
blob("local", "byte", ptop(), succ, _s)
*
_s != "0"
*
b_ls("host",ptop(),_s,"0")
* 
blob("local", "byte", ptop(), run, "1")
*
blob("local", "byte", ptop(), dataIn, _x)
* 
MFCSlot()
*
MFC(0,empty())
*
array("local", "byte", ptop(), dataIn, "0", buf_size)
* 
!GT(0,buf_size)
} 
{ 
}
