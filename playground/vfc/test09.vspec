f
{ array("local", "byte", ptop(), a, sz) * array("host", "byte", ptop(), b, sz) * !LE(0, sz) * MFCSlot() * MFC(t, empty()) }
{ array("local", "byte", ptop(), a, sz) * array("host", "byte", ptop(), b, sz) * MFCSlot() * MFC(t, empty()) }
