/** Remove unreachable nodes and list segments on the LHS. **/
abstraction node_garbage_abs:
  node(_i, ?ip, ?in, ?e) ~~> 
where
  _i notincontext

abstraction dseg_singleton_garbage_abs:
  dseg_ne(_i, ?ip, ?in, _i, ?l) ~~>
where
  _i notincontext

